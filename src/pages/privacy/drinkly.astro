---
import PrivacyLayout from "@/layouts/PrivacyLayout.astro";
import { getAppBySlug } from "@/data/apps";

const app = getAppBySlug("drinkly");
if (!app) {
  throw new Error("Drinkly app data is missing.");
}
---

<PrivacyLayout app={app} />
