---
import AppDetailLayout from "@/layouts/AppDetailLayout.astro";
import { getAppBySlug } from "@/data/apps";

const app = getAppBySlug("secure-qr-scanner");
if (!app) {
  throw new Error("Secure QR Scanner app data is missing.");
}
---

<AppDetailLayout app={app} />
